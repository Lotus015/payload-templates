# Database Configuration
# For local development with direct PostgreSQL connection:
DATABASE_URL=postgres://user:password@localhost:5432/payload_db

# For Vercel with connection pooling (Vercel Postgres / Neon / Supabase):
# DATABASE_URL=postgres://user:password@host:5432/payload_db?sslmode=require&pgbouncer=true
#
# Connection pooling notes:
# - Vercel Postgres: Use the pooled connection string from your Vercel dashboard
# - Neon: Add ?sslmode=require to your connection string
# - Supabase: Use the "Connection pooling" URL from Project Settings > Database
#
# For non-pooled connections (e.g., migrations), some providers offer a separate URL:
# DIRECT_URL=postgres://user:password@host:5432/payload_db?sslmode=require

# Payload CMS
# Generate a secure secret with: openssl rand -base64 32
PAYLOAD_SECRET=your-secret-key-min-32-characters-long

# Next.js
NEXT_PUBLIC_SITE_URL=http://localhost:3000
